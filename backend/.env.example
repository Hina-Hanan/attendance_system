# Database Configuration
# Use your actual PostgreSQL username and password. Use 127.0.0.1 (not localhost) on Windows to avoid IPv6 auth issues.
DATABASE_URL=postgresql://postgres:Hina123@127.0.0.1:5432/attendance_db

# Face Recognition Settings
FACE_MATCH_THRESHOLD=0.6
MIN_FACE_IMAGES_REQUIRED=3
MAX_FACE_IMAGES_REQUIRED=4

# Spoof Prevention Settings
SPOOF_CHECK_FRAMES=5
BLINK_DETECTION_THRESHOLD=0.25
HEAD_MOVEMENT_THRESHOLD=0.1

# API Settings
API_V1_PREFIX=/api/v1
# For production, add your frontend URL (e.g. Vercel). Example:
# CORS_ORIGINS=["https://your-app.vercel.app","http://localhost:3000"]
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Security
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
